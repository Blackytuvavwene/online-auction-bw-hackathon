import { component$ } from "@builder.io/qwik";

export default component$(() => {
  return (
    <div className='flex flex-col justify-center h-[90vh] items-center gap-10'>
      <p>Register page</p>
    </div>
  );
});