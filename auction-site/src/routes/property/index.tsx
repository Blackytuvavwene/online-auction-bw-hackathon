import { component$ } from "@builder.io/qwik";

export default component$(()=>{
    return (
        < >
        <section className="">
            <h1>Property</h1>
        </section>
        </>
    );
})